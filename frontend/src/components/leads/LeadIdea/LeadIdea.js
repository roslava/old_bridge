export default function LeadIdea({ text }) {
    return (
        <div className="w-full bg-old-white-50 text-black py-4 px-6 rounded-[8px] text-lg font-semibold">
          <p>{text}</p>
        </div>
    )
}