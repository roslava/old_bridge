import nextConfig from "../../next.config.mjs";

/** @type {import("pliny/config").PlinyConfig } */
const siteMetadata = {
    siteTitle: 'Немецкий мост',
    description: 'Немецкий мост',
    stickyNav: true,
    headerTitle: 'BridgeTown',
    theme: 'system',
 }

export default siteMetadata;