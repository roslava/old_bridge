'use strict';

/**
 * photo-lens-slider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::photo-lens-slider.photo-lens-slider');
